require 'test/unit'
require File.dirname(__FILE__) + '/../lib/meta_tag_helper'

class ToCsvTest < Test::Unit::TestCase
  
  # TODO: Write tests for the following
  # * Array should be of AR instances
  # * Array should all be of same class
  # * Should create one row per item
  # * Should be able to definte :only option
  # * Should be able to define :except option
  # * Should be able to disable headers  
  
  def test_truth
    true
  end
  
end

<h1>New resource</h1>

<%= error_messages_for :resource %>

<% form_for(:resource, :html => { :multipart => true }, :url => resources_path) do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>
  <%= submit_tag "Create" %>
<% end %>

<%= link_to 'Back', resources_path %>